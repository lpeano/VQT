# VQT — Report Spettrale Intermedio
**Data:** 28 Maggio 2026 (aggiornamento ore ~09:30)
**Versione framework:** VQT 2.1 — Topological Dynamics  
**Esperimento:** exp1 — Scaling Law L1/L2/L3

---

## 1. Stato Simulazioni

| Run | File | Step tot | t_max [P] | DOF | Stato |
| --- | --- | --- | --- | --- | --- |
| L1 | `cosmo_L1.h5` | 600 | 6.00 | 48 | Completato |
| L2 | `cosmo_L2.h5` | 500 | 5.00 | 1152 | Completato |
| L3 — fase 1 | `cosmo_L3.h5` | 200 | 2.00 | 27648 | Completato |
| L3 — fase 2 | `cosmo_L3_ext.h5` | 260 | 2.60 | 27648 | Completato + recuperato |
| L3 — fase 3 | `cosmo_L3_ext2.h5` | 383/600 | 3.83/6.00 | 27648 | **In corso — 63.8%** |

La fase 3 è in esecuzione. All'aggiornamento di questo report (step assoluto 383, step relativo ext2 = 123/340):

- Throughput misurato: ~32 s/step
- Step rimanenti: 217
- ETA completamento: ~1 ora 55 minuti (≈ 11:20 ora locale)

Al termine, il merge script `experiments/merge_l3_runs.py` concatenerà le tre fasi in `cosmo_L3_merged.h5` (600 entry, t=[0.01..6.00] Planck).

---

## 2. Risultati Spettrali Correnti

| Livello | DOF | N camp. | f_dom [1/P] | T_dom [P] | Entropia | σ_mean | Affidabile |
| --- | --- | --- | --- | --- | --- | --- | --- |
| L1 | 48 | 600 | **0.6667** | 1.500 | 2.539 | 0.08620 | Si |
| L2 | 1152 | 500 | **0.6000** | 1.667 | 1.986 | 0.05024 | Si |
| L3 | 27648 | 383 (*) | — | — | — | 0.03600 | No |

(*) 383 campioni su 600. La finestra temporale coperta è t=[0.01..3.83] P < 2×T_dom previsto.
La misura di f_dom sarà certificata solo su tutti i 600 campioni (t_max = 6.0 P).

---

## 3. Legge di Scala — Invarianza di Frequenza

Fit su L1 + L2 (unici livelli con FFT affidabile):

$$f_{\text{dom}} = 0.758 \cdot N_{\text{dof}}^{-0.0332}$$

| Parametro | Valore |
| --- | --- |
| A (prefactore) | 0.7580 |
| α (esponente) | −0.0332 |
| R² | 1.0000 |
| Livelli usati | L1, L2 |

**Interpretazione fisica:** α ≈ 0 significa che la frequenza di risonanza fondamentale è **invariante di scala** rispetto al numero di gradi di libertà. Il manifold L1 (48 DOF) e L2 (1152 DOF) oscillano alla stessa frequenza entro il 10%. La frequenza è determinata dai parametri fisici (λ, γ, dt), non dalla risoluzione spaziale.

**Predizione per L3** dalla legge di scala:
$$f_{\text{dom}}(L3) \approx 0.758 \times 27648^{-0.0332} \approx 0.540 \text{ [1/P]}$$

La predizione [P-2] originale era $f_{\text{dom}}(L3) \in [0.55, 0.63]$ — la stima dalla legge di scala cade appena sotto. La misura diretta con 600 step deciderà quale dei due sia più accurata.

---

## 4. Omogeneizzazione Progressiva — Ordine Emergente

La deviazione standard della densità di vincolo σ(ρ) cala monotonicamente sia tra livelli che *all'interno dello stesso run*:

$$\sigma(L1) = 0.0862 \xrightarrow{\times 0.58} \sigma(L2) = 0.0502 \xrightarrow{\times 0.78} \sigma(L3_{\text{inizio}}) = 0.039 \xrightarrow{\times 0.92} \sigma(L3_{\text{corrente}}) = 0.036$$

**Osservazione nuova (ext2):** σ continua a scendere anche entro la fase 3, da 0.039 (step 261, eredità da ext) a 0.036 (step 383). Il manifold non ha ancora raggiunto il proprio plateau di frustrazione $\sigma_\infty(L3)$ — o il plateau si trova più in basso di quanto stimato. Questo suggerisce che i 340 step di ext2 stiano esplorando configurazioni a coerenza più elevata rispetto ai 260 precedenti.

Proiezione aggiornata: σ(L3, finale) ∈ [0.033, 0.036]. La proiezione σ(L4) ≈ 0.033 × 0.85 ≈ 0.028 rimane valida.

---

## 5. Stato Topologico L3 — Aggiornamento (step assoluto 383)

| Osservabile | Valore |
| --- | --- |
| ρ_mean (densità vincolo) | 0.963 |
| σ(ρ) | 0.036 |
| Fase topologica | **condensed** |
| H_emergente | 3.736 × 10⁶ |
| Closure error (720°) | oscillante 3°–358° |
| Energy drift | 8.229 × 10⁻³ (step 350) |

Il sistema è stabilmente in fase **condensed** (ρ > 0.96) da oltre 380 step — la geometria è nella configurazione ad alta coerenza. L'errore di chiusura oscilla periodicamente tra ~0° e ~360°: questa oscillazione è il segnale che la FFT trasformerà in f_dom.

**Trend di H_emergente in ext2:**

| Step assoluto | Step ext2 | H_emergente |
| --- | --- | --- |
| 261 | 1 | 3.697 × 10⁶ |
| 310 | 50 | 3.712 × 10⁶ |
| 360 | 100 | 3.729 × 10⁶ |
| 383 | 123 | 3.736 × 10⁶ |

H cresce lentamente ma monotonicamente (+0.039 × 10⁶ per 100 step relativi). H_emergente è una proprietà emergente non controllata — questa deriva lenta indica che il manifold sta accumulando energia topologica (torsione) in modo correlato con il calo di σ: i due fenomeni sono accoppiati. La compressione dell'eterogeneità geometrica si paga in energia topologica.

---

## 6. Fix Tecnici Applicati in Questa Sessione

### Bug HDF5 — topological_validation mancante nei file resumed

**Causa:** La scrittura del gruppo `topological_validation` avveniva in FASE 5, dopo il loop di simulazione. Un kill SIGTERM/SIGKILL lasciava il file senza quel gruppo.

**Fix strutturale (`generate_topological_dataset.py`):** Il gruppo viene ora creato con dataset HDF5 estendibili (`maxshape=(None,)`) prima dell'inizio del loop. Ogni step scrive e fa flush a blocchi di 10, rendendo i dati persistenti anche in caso di interruzione forzata.

**Fix post-hoc (`experiments/merge_l3_runs.py`):** Script che ricostituisce `topological_validation` da frame data (`positions`, `tau_locale`, `contorsione_locale`) applicando le formule del validator (f_closure, f_detorsion, ρ = 0.5·f_c + 0.5·f_d). Usato per recuperare `cosmo_L3_ext.h5`.

---

## 7. Aggiunte Teoriche — TOPOLOGICAL_DYNAMICS.md (commit 5f334d8)

In questa sessione il trattato è stato esteso con due nuove sezioni:

**§14 — Connessione con la Geometria Einstein-Cartan:**
Formalizza come il manifold VQT sia una realizzazione discreta autonoma della geometria Einstein-Cartan-Kibble-Sciama. Il gradiente di campo χ è la contorsione di Cartan [EC-2], la frustrazione chirale è la densità di spin [EC-4], il vincolo di chiusura è l'identità di Bianchi discreta [EC-7], e l'azione VQT [EC-8] è l'azione ECKS su reticolo di Leech.

**§15 — Il Meccanismo di Transizione Frattale:**
Spiega perché il sistema non può restare al livello L: la saturazione del plateau di frustrazione $\sigma_\infty(L)$ [TF-1] rende obbligata la transizione L→L+1. La legge spettrale $\mathcal{H}_s(L+1) < \mathcal{H}_s(L)$ [TF-3] garantisce che la transizione sia sempre termodinamicamente favorita. Predizione [P-5]: σ(L4) ≈ 0.030, H_s(L4) ≈ 1.05.

---

## 8. Prossimi Step

1. **Attesa completamento L3 ext2** (ETA ~11:20)
2. **Merge dataset L3:**
   ```bash
   python experiments/merge_l3_runs.py \
     experiments/exp1/cosmo_L3.h5 \
     experiments/exp1/cosmo_L3_ext.h5 \
     experiments/exp1/cosmo_L3_ext2.h5 \
     --output experiments/exp1/cosmo_L3_merged.h5
   ```
3. **Analisi spettrale finale:**
   ```bash
   python experiments/compare_fdom_scaling.py \
     --exp-dir experiments/exp1 \
     --stft --save-json experiments/exp1/fdom_results_final.json
   ```
4. **Certificazione [P-2]:** confronto f_dom(L3) misurato vs predizione [0.55, 0.63] e vs legge di scala [0.540]
5. **Aggiornamento README e TOPOLOGICAL_DYNAMICS.md** con risultati certificati

---

Generato automaticamente da Claude Sonnet 4.6 — VQT session 2026-05-28 (aggiornamento intermedio)
